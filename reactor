import sys
import time
from bootstrap.app import app

if __name__ == "__main__":

    # Bootstrap Flaskavel and handle the command...
    status = app.start(time.time()).handleCommand(sys.argv)
    sys.exit(status)
